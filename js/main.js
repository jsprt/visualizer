var height = 600, width = 900;
var container = d3.select("body")
                            .append("svg")
                            .attr("height",height)
                            .height("width",width);